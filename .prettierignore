# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing
# generated by nx release
CHANGELOG.md
# Generated, and checked by `format:check --all` unless named here: the React Router
# route typegen and the app build output. Both are gitignored — formatting them is
# noise, and a repo-wide check that trips on them is a check people learn to skip.
/apps/blog/.react-router
/apps/blog/build
