# CLAUDE.md

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. The operating contract for **all**
agents lives in `AGENTS.md` (+ the `.agent/*` spokes) — this file just imports it so
Claude and the other agents share one source. Add Claude-only notes below the import
if ever needed; keep shared rules in `AGENTS.md`.

@AGENTS.md
